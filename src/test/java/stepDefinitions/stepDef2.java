package stepDefinitions;

import com.microsoft.playwright.Page;
import io.cucumber.java.en.Then;
import utils.PlaywrightManager;

import static org.junit.jupiter.api.Assertions.assertTrue;


public class stepDef2 {

//    private final Page page = PlaywrightManager.getPage();
//
//    @Then("the URL should contain {string} in StepDefinitions")
//    public void theURLShouldContainInStepDefinitions(String expectedText) {
//        String currentUrl = page.url();
//        assertTrue(currentUrl.contains(expectedText), "URL does not contain the expected text!");
//    }
}
